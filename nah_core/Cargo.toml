[package]
name = "nah_core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Core engine for `nah` — compile-time Clean Code quality gates. Provides process_gate() and define_gates! macro."

[dependencies]
proc-macro2 = "1.0"
quote = "1.0.45"
syn = {version = "2.0.117", features = ["full"]}

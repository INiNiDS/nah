[package]
name = "nah_macros"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Pre-defined Clean Code violation markers for `nah` — the entire set of 24 attribute macros."

[lib]
proc-macro = true

[dependencies]
nah_core = { path = "../nah_core", version = "0.1.1" }
proc-macro2 = "1.0"

[workspace]
members = [
    ".",
    "nah_core",
    "nah_macros",
]

[package]
name = "nah"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Compile-time Clean Code quality gates for Rust — re-exports nah_core and nah_macros."

[dependencies]
nah_core = { path = "nah_core" }
nah_macros = { path = "nah_macros" }

[dev-dependencies]
trybuild = "1.0.111"

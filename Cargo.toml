[workspace]
members = [
    ".",
    "nah_core",
    "nah_macros",
]
[workspace.package]
version = "0.1.2"
edition = "2024"
license = "MIT"

[package]
name = "nah"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Compile-time Clean Code quality gates for Rust — re-exports nah_core and nah_macros."

[dependencies]
nah_core = { path = "nah_core", version = "0.1.2" }
nah_macros = { path = "nah_macros", version = "0.1.2" }

[dev-dependencies]
trybuild = "1.0.111"

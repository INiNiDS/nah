[package]
name = "nah"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
proc-macro = true

[dependencies]
syn = {version = "2.0.117", features = ["full"]}
quote = "1.0.45"

[dev-dependencies]
trybuild = "1.0.111"
